class Category 
{
    constructor(id, categoryTitle, vectorName)
    {
        this.id = id;
        this.categoryTitle = categoryTitle;
        this.vectorName = vectorName;
    }
}

export default Category;